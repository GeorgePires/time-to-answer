<h1 class="h3 mb-2 text-gray-900">AdminsBackoffice::Subjects#index</h1>
<!-- DataTales Example -->
<div class="card shadow mb-4 border-bottom-primary">
  <div class="card-header py-3 border-left-primary">
    <h6 class="m-0 font-weight-bold text-primary">Subjects list</h6>
  </div>
  <div class="card-body">
    <div class="table-responsive">
      <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
        <thead>
          <tr>
            <th>Id</th>
            <th>Description</th>
            <th class="actions-table">
              <%= link_to new_admins_backoffice_subject_path, class: "btn btn-info btn-icon-split " do %>
                <span class="icon text-white-50">
                  <i class="fa-solid fa-user-plus"></i>
                </span>
                <span class="text">New Subject</span>
              <% end %>
            </th>
          </tr>
        </thead>
        <tbody>
          <% @subjects.each do |subject| %>
            <tr>
              <td><%= subject.try(:id) %></td>
              <td><%= subject.try(:description) %></td>
              <td class="actions-table">
                <%= link_to edit_admins_backoffice_subject_path(subject), class: "btn btn-primary" do %>
                 <i class="fa-solid fa-pen-to-square"></i>
                <% end %>

                <%= link_to admins_backoffice_subject_path(subject), method: :delete, class: "btn btn-danger",
                                 data: { confirm: "Are you sure?" } do %>
                  <i class="fas fa-trash"></i>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @subjects %>
    </div>
  </div>
</div>
